const Loader = () => {
    return (
        <section className="loader-container flex-row">
           <h2>Loading map...</h2>
            <div className="loader"></div>
        </section>
    )
}

export default Loader
